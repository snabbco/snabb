#!/bin/bash
fabric_dir=$(cd `dirname $0`/..; pwd)

export LD_LIBRARY_PATH=$fabric_dir/src/c
exec luajit -e "package.path = package.path .. \";$fabric_dir/src/lua/?.lua\"" \
            $fabric_dir/src/lua/tester.lua "$@"

