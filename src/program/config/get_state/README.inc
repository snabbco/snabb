Data Format Usage:
  snabb config get-state <instance name> <path>

This command takes in a instance name and a path to inside the schema and will
display the statistics at that path. 

Example usage:

  $ snabb config get-state lwaftr1 /snabb-softwire-v1/softwire-state/
