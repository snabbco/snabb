softwire-config {
  binding-table {}
  external-interface {
    allow-incoming-icmp false;
    error-rate-limiting {
      packets 600000;
    }
    reassembly {
      max-fragments-per-packet 40;
    }
  }
  internal-interface {
    allow-incoming-icmp false;
    error-rate-limiting {
      packets 600000;
    }
    reassembly {
      max-fragments-per-packet 40;
    }
  }
}
