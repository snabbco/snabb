Usage:
  snabb wall <subcommand> <arguments...>
  snabb wall --help

Available subcommands:

  pcap    Analyze a .pcap capture and report statistics

Use --help for per-command usage. Example:

  snabb wall pcap --help
